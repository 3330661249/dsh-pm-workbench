# Reviewed platform locks

No reviewed locks are committed yet. A frozen run fails closed as `INCONCLUSIVE_LOCK` when `locks/<platform-key>/<case-id>.package-lock.json` is absent. Resolve-mode locks remain under the ignored run root until reviewed and copied here in a separate change.
